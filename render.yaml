services:
  - type: web
    name: yourday
    runtime: node
    buildCommand: apt-get update -qq && apt-get install -y ffmpeg && npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
